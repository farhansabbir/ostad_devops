# # Required metadata for SonarCloud
# sonar.projectKey=farhansabbir_ostad_devops
# sonar.organization=farhansabbir
# sonar.host.url=https://sonarcloud.io

# # Displayed in the SonarCloud UI
# sonar.projectName=Ostad DevOps Next.js App # Give it a meaningful name
# sonar.projectVersion=1.0 # Or use a dynamic version like from package.json if you have a build step that can inject it

# # Path to source code - required
# # '.' means the scanner will analyze all files in the directory where this properties file resides (repository root)
# sonar.sources=.

# # Encoding of the source code. Recommended to explicitly set for JS/TS
# sonar.sourceEncoding=UTF-8

# # --- Optional but highly Recommended for Next.js/JS/TS Projects ---

# # Test Coverage (using LCOV format, e.g., from Jest/Istanbul)
# # You need to ensure your test runner (e.g., Jest) generates an LCOV report.
# # Example: If Jest outputs to 'coverage/lcov.info'
# sonar.javascript.lcov.reportPaths=coverage/lcov.info

# # Test files inclusion (so SonarCloud knows these are tests, not source files)
# # Adjust these patterns to match your test file naming conventions (e.g., .test.js, .spec.ts)
# sonar.tests=.
# sonar.test.inclusions=**/*.test.js,**/*.test.jsx,**/*.spec.js,**/*.spec.jsx,**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx

# # Exclusions (paths to ignore from analysis - node_modules, build outputs, static assets etc.)
# # These are common exclusions for Next.js and Node.js projects.
# sonar.exclusions=\
#   node_modules/**/*,\
#   .next/**/*,\
#   out/**/*,\
#   .yarn/**/*,\
#   .pnp.cjs,\
#   .pnp.loader.cjs,\
#   public/**/*,\
#   coverage/**/*,\
#   dist/**/*,\
#   build/**/*,\
#   temp/**/*,\
#   e2e/**/*,\
#   cypress/**/*,\
#   **/*.min.js,\
#   **/*.d.ts,\
#   **/*.scss,\
#   **/*.css,\
#   **/*.json,\
#   **/*.lock

# # TypeScript specific properties (if your Next.js project uses TypeScript)
# # This points SonarQube to your tsconfig.json for better type-aware analysis
# sonar.typescript.tsconfigPath=./tsconfig.json

# # If you have Jest/JUnit test reports (e.g., from --ci or --jest-junit)
# # sonar.junit.reportPaths=junit.xml,test-results.xml

# # Optional: To skip Node.js provisioning if you want to ensure it uses the system Node.js
# # sonar.scanner.skipNodeProvisioning=true

sonar.projectKey=farhansabbir_ostad_devops
sonar.organization=farhansabbir

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=ostad_devops
#sonar.projectVersion=1.0


# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8